export type BannerPreset = {
  id: string;
  name: string;
  size: { width: number; height: number };
  category: string;
};

export type TemplateCard = {
  id: string;
  title: string;
  category: string;
  gradient: string;
};

