{
  "name": "vistaarwater-monorepo-runner",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend,studio -c green,cyan,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:studio\"",
    "dev:backend": "powershell -NoProfile -Command \"if (Test-Path 'backend/.venv/Scripts/python.exe') { & 'backend/.venv/Scripts/python.exe' -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend } else { python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend }\"",
    "dev:frontend": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 5173",
    "dev:studio": "npm --prefix studio-next run dev -- --hostname 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
